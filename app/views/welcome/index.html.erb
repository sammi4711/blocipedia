<h1>Welcome to Blocipedia!</h1>
<p><%= link_to "View Wikis", wikis_path, class: 'btn btn-success' %></p>

  <div class="upgrade">
    <% if current_user.role == 'standard_member'%>
      <p>Want to get more out of Blocipedia? Find out about our PREMIUM MEMBERSHIPS below!</p>
      <%= button_to "Click Me!", charges_path %>
    <% end %>
  </div>

  <div class="downgrade">
    <% if current_user.role == 'premium_member'%>
      <p>Not enjoying your Premium membership? Click below to downgrade.</p>
      <%= button_to "Are you sure you want to Click Me?", charges_path %>
    <% end %>
  </div>