<%= form_for ([@wiki, @collaborator]) do |f| %>
  <div class="form-group">
    <%= f.label :user %>
    <%= f.text_field :user, class: 'form-control', placeholder: "Enter User Email", as: :fake %>
  </div>
<% end %>